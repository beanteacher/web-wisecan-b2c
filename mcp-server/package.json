{
  "name": "mcp-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "tsx mcp-stdio.ts",
    "typecheck": "tsc --noEmit",
    "prisma:generate": "prisma generate",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@prisma/client": "^6.16.0",
    "dotenv": "^16.0.0",
    "prisma": "^6.16.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "@types/node": "^20.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0",
    "vitest": "^4.1.1"
  }
}
